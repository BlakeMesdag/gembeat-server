<!DOCTYPE html>
<html>
<head>
  <title>Gembeat Server</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <a class="brand" href="/" style="margin-left: 10px">Gembeat</a>

      <ul class="nav">
        <li class="<%= params[:controller] =~ /applications/ ? "active" : "" %>">
          <%= link_to "Applications", applications_url %>
        </li>
        <li class="<%= params[:controller] =~ /dependencies/ ? "active" : "" %>">
          <%= link_to "Dependencies", dependencies_url %>
        </li>
        <li class="<%= params[:controller] =~ /vulnerabilities/ ? "active" : "" %>">
          <%= link_to "Vulnerabilities", vulnerabilities_url %>
        </li>
      </ul>

      <ul class="nav pull-right">
        <li><%= link_to "Logout", logout_url %></li>
      </ul>
    </div>
  </div>

  <%= yield %>
</div>

</body>
</html>
